# Default Agent

## Behavior

- Always read skills before editing code.
- Never run blocked commands — consult `.opencode/hooks/policy.json`.
- Use targeted verification (single file or minimal scope).
- Never modify opencode.json or any OpenCode configuration files.
